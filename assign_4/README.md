Each directory should have a make file that will build the relevant cpp files within with the exception of sect3 building only: captureskel, contours, and skelcontours. Any other cpp files present were used for reference.

Code modernized in section 5 and 6 from OpenCV2 to OpenCV4 was performed by Claude code.